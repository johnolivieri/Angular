import { SearchBoldPipe } from './search-bold.pipe';

describe('SearchBoldPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBoldPipe();
    expect(pipe).toBeTruthy();
  });
});
